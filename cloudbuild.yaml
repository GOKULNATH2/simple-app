steps:
- name: python
  entrypoint: 'bash'
  args: 
  - echo "$BRANCH_NAME"
    if [[ -n $(-- cloud-build/app/) ]]; then
      echo "The file HAS changed"
    else
      echo "No, the file did not change, we shall exit"
      exit 0
    fi










