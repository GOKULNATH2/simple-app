steps:
- name: python
  entrypoint: 'bash'
  args: 
  - '-c'
  - |
    echo "$(git ls-tree -r master)"
    echo "$COMMIT_SHA"
    echo "$REVISION_ID"
    echo "$(git diff-tree --no-commit-id --name-only -r $COMMIT_SHA)"
    echo "$BRANCH_NAME"
    echo "$(git log -1 --skip 1)"
    echo "$(git diff --name-only $COMMIT_SHA master)"
    echo "$(git branch)"
    echo "$(git diff --pretty="" --name-only $COMMIT_SHA)"
    










