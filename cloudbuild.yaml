steps:
- name: python
  args: 
  - |
    echo $BRANCH_NAME
    if [[ -n $_FILE_HAS_CHANGED ]]; then
      echo "The file HAS changed"
    else
      echo "No, the file did not change, we shall exit"
      exit 0
    fi










